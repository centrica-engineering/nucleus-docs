---
title: Date picker
---

export const componentName = "ns-datepicker";

import Specification from '@components/specification.astro';
import Placement from '@components/placement.astro';
import CompDetails from '@components/cem-description.astro';
import ExampleViewer from '@components/ExampleViewer.astro';
import RelatedLinks from '@components/related-links.astro';
import { Tabs, TabItem } from '@astrojs/starlight/components';

## Overview

<CompDetails name={componentName} />

The `ns-datepicker` wraps the `ns-inputter` component. Guidelines defined for the `ns-inputter` should also be adhered to when using this component. See [`<ns-inputter>`](/components/ns-inputter) documentation.

### Examples

<Tabs>
  <TabItem label="Standard">
    <ExampleViewer type="standard" />
  </TabItem>
  <TabItem label="Standard min max">
  Use the `min` and `max` attributes attributes to define a selectable range between two specific dates, or use each attribute separately.
    <ExampleViewer type="standard-min-max" />
  </TabItem>
  <TabItem label="Mask">
  Use `type="mask"` when the calendar feature is not required.
    <ExampleViewer type="mask" />
  </TabItem>
  <TabItem label="Mask min max">
  Use the `min` and `max` attributes attributes to define a selectable range between two specific dates, or use each attribute separately.
    <ExampleViewer type="mask-min-max" />
  </TabItem>
</Tabs>

## Guidance

:::do
- Use the `standard` type for picking a date from a range.
- Use the `standard` type for picking a date that the user might not be sure of (e.g. Next Friday).
- Use the `masked` type for picking a date that is well known to the user (e.g. Date of birth).
:::

:::dont
- Change the date format.
:::

## Implementation

### Placement

<Placement name={componentName} />

### Specification

<Specification name={componentName} />
